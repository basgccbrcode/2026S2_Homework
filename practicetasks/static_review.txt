Static members in Java belong to a class and not a specific method, while each object has a different instance.

* utility classes are best used to improve static methods (example: mathutil).

Static variables represent a global state.